# Local gradle + loom caches (must never be committed)
.gradle/
.gradle-user-home/
.gradle_user_home_1_21_2/
build/
run/
logs/
crash-reports/
out/
temp_tectonic_extract/
_restore_tmp/
_mcsrc_extract/
gradle-8.8/
gradle-8.8-bin.zip
bin/

# Windsurf: keep skills tracked, ignore other state
.windsurf/*
!.windsurf/skills/
!.windsurf/skills/**

# IDE
.idea/
.vscode/
*.iml

# Local media (do not commit)
Gallery for Modrinth/
*.mp4

# Atlas desktop app local artifacts
tools/atlas/desktop-app/node_modules/
tools/atlas/desktop-app/dist/
tools/atlas/desktop-app/.electron-builder/

# Atlas generated runs
run-headless/latdev/atlas-runs/

run-headless/warm_ocean_debug_trace.txt

# run-headless runtime state (regenerated on every headless gen — never commit)
run-headless/world/
run-headless/dimensions/
run-headless/.fabric/
run-headless/.mixin.out/
run-headless/config/
run-headless/mods/
run-headless-base/

# Generated manual atlas image exports (not source)
/Manual atlas/

# Claude Code local state (per-user, not shared)
.claude/settings.local.json
.claude/*.lock

# ── NEVER TRACK (2026-08-07 incident — see CLAUDE.md ⛔ section) ──
_mcsrc_extract/
_mcsrc_yarn/
_mcsrc*/
docs/binder/
run-headless/
tmp/
*.7z
*.zip
compile-info.log
codex_task_*.txt
.codex/
Gallery for Modrinth/

# Atlas Viewer -- internal dev tooling, never public (maintainer ruling, 2026-08-28).
# The atlas GENERATOR stays tracked; only the viewer is withheld. Untracked rather than
# sanitised: a viewer that reads local runs will keep accreting local paths and world data,
# so the durable fix is that it is not in the repo at all.
atlas_viewer.ps1
tools/atlas/viewer.html
tools/atlas/viewer/
tools/atlas/viewer_api_server.py
