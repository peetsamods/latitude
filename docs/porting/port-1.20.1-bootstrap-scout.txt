# Latitude 1.3.0 -> Minecraft 1.20.1 Bootstrap Scout

Root: `/Users/joolmac/CascadeProjects/Latitude (Globe)`
Current branch: `docs/port-bootstrap-1.21.1-hotfix-lessons`
Current HEAD: `361f252`
Current tags: `save/port-bootstrap-1.21.1-hotfix-lessons`
Tree state: tracked source/build/resource files clean; `gradlew` has mode-only dirt; `docs/porting/latest-port-bootstrap-audit.txt` was generated by the bootstrap audit

## Objective

Port Latitude 1.3.0 to Minecraft 1.20.1.

## Donor recommendation

- donor commit/tag: `9d12f25` / `save/1.21.1-hotfix-final-audit`
- why this donor: it is the latest real 1.21.1 gameplay/client hotfix audit head, while current HEAD is a docs/bootstrap branch
- whether it includes F9 parity, existing-world loading, EW haze readability, runtime proof, and artifact purity lessons: yes; its history includes `save/1.21.1-f9-ui-parity-hotfix`, `save/1.21.1-existing-world-loading-hotfix`, `save/1.21.1-ew-haze-hud-occlusion`, and the final hotfix audit

## Bootstrap result

- bootstrap script ran: yes
- generated report path, if any: `docs/porting/latest-port-bootstrap-audit.txt`
- first failing phase: metadata still targets 1.21.1, so the 1.20.1 port has not reached dependency resolution yet

## Metadata target

- Minecraft version: `1.21.1`
- mappings: `1.21.1+build.3`
- Fabric API: `0.115.6+1.21.1`
- Fabric Loader: `0.17.3`
- Loom: `1.14.10`
- Java: `21`
- mod version: `1.3.0+1.21.1`

## 1.20.1 risk buckets

- dependency/mapping drift: high; all metadata currently targets 1.21.1 and must be scouted for 1.20.1-compatible versions first
- compile API drift: unknown until metadata is retargeted and compile is run
- mixin descriptor drift: high; `populateBiomes`, `carve`, `setBlockState`, `CreateWorldScreen`, `WorldCreator`, `LevelLoadingScreen`, `DownloadingTerrainScreen`, `InGameHud`, `renderHotbar`, and `BackgroundRenderer` are present
- worldgen live hook: high; `ChunkGeneratorPopulateBiomesMixin` targets private populate-biomes descriptors and chunk biome population calls
- create-world UI: high; bespoke create-world code and `WorldCreator` mixins are present
- loading overlay: high; new-world and existing-world loading overlay mixins are present
- F9/HUD Studio: high; `LatitudeSettingsScreen`, `LatitudeHudStudioScreen`, and HUD render suppression paths are present
- EW haze/HUD readability: high; `EwSandstormOverlayHud` and `InGameHudMixin` are present
- `/latdev`: high; dev tooling and process-launch source paths exist and must remain out of public jars
- artifact purity: high; jar exclusions exist, but must be re-proven after any 1.20.1 metadata or packaging change

## First failing phase

METADATA DRIFT

## Recommended next slice

With approval, create the 1.20.1 port branch from `9d12f25` / `save/1.21.1-hotfix-final-audit`, then do a metadata-only 1.20.1 dependency scout and stop at the first dependency or compile blocker.

## Do not touch next

- Java source behavior
- worldgen and biome logic
- create-world UI behavior
- loading overlay behavior
- EW haze or HUD behavior
- release packaging and jar exclusions
- `gradlew`

