# Latitude 1.3.0 -> 1.20.1 Populate Biomes Mixin Retarget

Root: `/Users/joolmac/CascadeProjects/Latitude (Globe)`
Branch: `port/1.3.0-1.20.1`
HEAD: `2c6fae0`

## Scope

Retargeted the populate-biomes mixin to the 1.20.1 registry API.

## Notes

- Replaced identifier-based biome registry lookups with 1.20.1 `RegistryKey<Biome>` lookups.
- Added a tiny helper to parse string biome IDs into registry keys.
- Preserved live biome hook intent and biome selection behavior.
- Compile proof no longer fails on `ChunkGeneratorPopulateBiomesMixin`.
- Remaining compile failures are unrelated buckets outside this slice.
