# 1.4 "Cohesive Horizons" worldgen backport (Stage 1) — Minecraft 1.21.11

`status: active` · scope: `worldgen` · workflow: `latitude-regression-fix-workflow`
Branch `port/1.4.0-beta-1.21.11`, baseline tag `v1.3.0+1.21.11-r2` (`0380b610`). Mappings: Yarn `1.21.11+build.1`, JDK 21.
Version: `1.4.0-beta.1+1.21.11`. Authored by the overnight backport thread (2026-06-06) per `docs/porting/HANDOFF-1.4-beta-backport.md`.

## Scope landed (Stage 1 / Bucket 1 — worldgen logic only)

This is an **algorithm transplant**, not a cherry-pick: the 1.4 source (26.1.2, Mojang mappings) was authored on a
**province/moisture** worldgen architecture that **does not exist in the 1.3.0 line** for 1.21.11 (no `ProvinceAuthority`,
no `Province` enum, no `enforceWarmProvinceFamily`, no moisture classifier). The pure-logic 1.4 changes were therefore
re-grounded onto the target's existing Yarn (`RegistryEntry<Biome>`) worldgen and validated map-based.

| 1.4 axis | source commit | status in 1.21.11 | how |
|---|---|---|---|
| Art VI `pickFrom` coherent noise | `da1bc0dd` | **LANDED (verbatim logic)** | `pickFrom` argmax over N coherent `ValueNoise2D` fields (`FALLBACK_COHERENCE_BLOCKS=128`, `FALLBACK_PICK_SALT`), replacing the `Math.floorDiv`+`hash64` cell-grid pick that was present and identical in this baseline. |
| Polar ice_spikes cap | `3f8e0366` | **LANDED (adapted)** | `keepPolarIceSpike` coherent cap (`POLAR_ICE_KEEP_THRESHOLD=0.45`, scale 256) in both `sanitizeLandBiome` overloads; source-source ice_spikes over the cap → `snowy_plains`. The baseline's `BAND_POLAR` block was *manufacturing* ice_spikes (forced forest/taiga→ice_spikes); rewired to `snowy_plains` (+`!path.contains("snowy")` guard) — Yarn idiom `getKey().map(k->k.getValue().getPath())`. |
| Equator overhaul: badlands gate + desert thinning | `91ffa425` + `10e10ffd` | **LANDED (re-grounded, province-independent)** | `demoteEquatorialBadlands`/`demoteEquatorialDesert` predicates collapsed into `demoteEquatorialDryBiomes` (both overloads), wired at the END of both `applyFinalSavannaClimateClamp` overloads before the savanna-tier pass. Keys on biome family + latitude ramp + coherent `ValueNoise2D` keep-noise (`BADLANDS_LAT_RAMP_LOW/HIGH=10/18`, `DESERT_LAT_RAMP_HIGH=12`, `DESERT_EQUATOR_KEEP_FRAC=0.40`). The WARM_DRY-province gate from the source is dropped (no province system here); the Earth-analog intent (no equatorial badlands; thinned-but-present equatorial desert; demote→savanna) is preserved exactly. |
| Warm-band rebalance (province wet-bias + WARM_WET variety + tier coherence 64→160 + badlands sub-province widen + `chooseBadlandsVariant`) | `8c73beab` | **DEFERRED (architecturally out of reach)** | Built entirely on `ProvinceAuthority.classifyWarm` / `enforceWarmProvinceFamily` / `Province` / moisture model — none exist in the 1.3.0 line. Porting it requires transplanting the whole province subsystem (Bucket-2-scale). The equatorial Earth-analog goal it targeted is delivered here by the province-independent equator gates above. `FALLBACK_COHERENCE_BLOCKS=128` is adopted; the `TIER_COHERENCE_BLOCKS=160` tier-coherence constant has no analog (no `pickFromWeighted` coherence tier in this baseline's selection path). |

## Proof (map-based, headless atlas, NO chunk pregen)

Seeds `2533348776566713405` + `1199119911991199`, `--size=small --radius=7500 --step=16 --emitbiomeindex=true`.
Metrics: `tools/atlas/sublatitude_dry_wet.py` + `tmp/tropical-badlands-gate-ace6e326/perbiome_sublat.py` (run from canonical repo).
**Note:** headless atlas is vanilla-biome-only (no BoP/Terralith/Promenade stack — that's a client/datapack runtime), so equatorial jungle share reads higher than the 26.1.2 BoP-stack figures; the §5 *qualitative* targets are what's validated.

### Per-band (seed 2533348776566713405 fix; % of land)
| band | badlands | desert | savanna | jungle |
|---|---|---|---|---|
| 0–5° | **0.00** | 5.45 | 6.61 | 82.37 |
| 5–10° | 0.00 | 1.37 | 7.50 | 87.62 |
| 10–15° | 0.00 | 1.10 | 6.83 | 86.92 |
| 24–30° | 0.01 | 1.99 | 33.97 | 57.84 |
| 30–35° (subtropical arid belt) | 0.00 | **54.98** | 23.68 | 0.26 |

Seed `1199119911991199` fix: 0–5° badlands **0.00%**, desert **0.00%**; 30–35° desert 53.92% (belt intact); equator jungle-dominant with savanna variety.

### Whole-world conservation (seed 2533348776566713405, biome_ids.png pixels, baseline vs fix)
- **ice_spikes 5.72% → 2.91%** (−24,676 px) — under its ~6% accent cap. Share moved to legitimate cold biomes (jagged_peaks +14,758, frozen_peaks +5,121, snowy_slopes +4,797; a downstream polar-elevation stage re-routes the converted picks, mirroring the 26.1.2 behavior).
- equator gates: badlands already 0% on this vanilla-headless seed (the leak was BoP-stack/seed-dependent in 26.1.2) → the gate is a **structural 0%-at-equator guarantee** here; desert −109 / savanna +113 (small, baseline equator desert already sparse).
- **Art X clean: 50 distinct biomes both sides — none vanished, none new.**

### Build
- `compileJava` → BUILD SUCCESSFUL (JDK 21).
- `build` → BUILD SUCCESSFUL; `remapJar` produced `build/libs/latitude-1.4.0-beta.1+1.21.11.jar` (refmap regime intact).
- SHA-256: `f408d5f6dd021065bfd2b868abb20cfe93464ccdecaba0c2aee11053b4dd14da`.

## Local-build note (do not regress)
`gradle.properties` keeps Julia's Windows `org.gradle.java.home`. Local macOS builds passed it via a temporary
edit to the local temurin-21 path, reverted before commit. To rebuild locally:
`env -u JAVA_TOOL_OPTIONS JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew build` with that property temporarily
pointed at the local JDK 21 (or removed).

## Deferred / next
- Warm-band province subsystem port (Bucket 2 / later full-1.4 stage) — see table.
- Custom-biome source-wrap mixins + world-entry client (explicitly excluded from Stage 1).
- Julia: push release tag, GitHub pre-release (Beta), Modrinth Beta upload. (Branch pushed; `save/*` tag local-only.)

---

# Stage 2 — custom-biome TAG admission (`1.4.0-beta.2+1.21.11`, 2026-06-06)

Per handoff §8. Version bumped `1.4.0-beta.1 → 1.4.0-beta.2+1.21.11`.

## 2a. Custom-biome tags — PROVEN (absent-mod-safe + Art X), placement NEEDS-JAR-VALIDATION

Mod set for the 1.21.11 / 26.1 ecosystem: **Biomes O' Plenty + Terralith + Promenade** (mirrors the proven 26.1.2
custom-biome set, commit `0965687c` family). **36 `required:false` entries** added across 9 `lat_*` tag files via
`scripts/apply_custom_biome_tags.py` (climate-placed by real-world analog):

| band / tag | mod entries added |
|---|---|
| `lat_subpolar_primary` (+9) | bop: tundra, snowy_maple_woods, coniferous_forest, fir_clearing, snowy_coniferous_forest, snowy_fir_clearing; terralith: siberian_taiga, wintry_forest; promenade: glacarian_taiga |
| `lat_subpolar_secondary` (+4) | terralith: siberian_grove, wintry_lowlands, snowy_maple_forest, snowy_shield |
| `lat_temperate_primary` (+2) | bop: seasonal_forest, maple_woods |
| `lat_temperate_secondary` (+10) | bop: woodland, old_growth_woodland, forested_field, orchard, pasture; terralith: birch_taiga, sakura_grove, sakura_valley; promenade: blush_sakura_grove, cotton_sakura_grove |
| `lat_temperate_accent` (+4) | bop: prairie; terralith: blooming_valley, lavender_valley, valley_clearing |
| `lat_arid_accent` (+1) | bop: dryland |
| `lat_trans_arid_tropics_1_secondary` (+2) | bop: scrubland, shrubland |
| `lat_trans_arid_tropics_2_secondary` (+3) | bop: rocky_shrubland, wasteland, wasteland_steppe |
| `lat_tropics_secondary` (+1) | bop: tropics |

All entries verified against the real biome registries of the BoP `26.1.2.0.2`, Terralith `26.1 v2.6.2`, and
Promenade `5.5.0` jars (`unzip … worldgen/biome/*.json`).

**PROVEN autonomously (headless atlas, vanilla — NO mods, NO pregen; seed `2533348776566713405`, small/R7500/step16):**
the datapack loads cleanly with all 36 `required:false` custom entries present, worldgen runs, and the world biome
inventory is **50 distinct biomes — byte-for-byte the Stage-1 vanilla set, zero non-`minecraft:` biomes, none vanished
(Art X clean).** This proves the tag additions are **absent-mod-safe and generation-neutral** when the provider mods
are absent (the only configuration verifiable on this machine).

**NEEDS-JAR-VALIDATION (for Julia):** the "biome appears in its tagged band, controlled share, coherent regions"
proof requires the provider mods present at **runtime**. The only BoP/Terralith/Promenade jars on this machine target
**MC 26.1.2** (`depends.minecraft = 26.1.2` / `~26.1-`, loader ≥0.18) and will **not** load on the 1.21.11 runtime
(loader 0.17.3) — so in-atlas mod-present validation could not be run here. The tag IDs are nonetheless real (jar-verified
above) and the 26.1.2 line already proved this exact placement set. **Action for Julia:** drop 1.21.11-compatible
BoP/Terralith/Promenade builds in `run-headless/mods` and re-run the atlas to confirm band placement + share.

## Build
- `runBiomePreview` (atlas) + `build` → BUILD SUCCESSFUL (JDK 21). Custom tags confirmed inside the remapped jar.
- Jar: `build/libs/latitude-1.4.0-beta.2+1.21.11.jar`
- SHA-256: `5338041f3ac2a49efcb3c723469d69bfa42bc4dbb78cc6ec82939012d1e72ba4`

## 2b. Source-wrap mixins + world-entry client — see RESULTS.md (attempted after 2a across all versions).

---

## Stage 3 — broaden custom-biome tag admission (2026-06-06, HANDOFF §9)

Added 101 `required:false` `lat_*` worldgen tag entries for four more popular biome mods, on top of the Stage-2 BoP/Terralith/Promenade set, via `scripts/apply_custom_biome_tags.py`:
- **BYG** 51 (IDs from `Potion-Studios/BYG` `BYGBiomes.java`, overworld+beach; nether/end excluded)
- **Terrestria** 20 (`TerrestriaBiomes.java`)
- **Wilder Wild** 23 (temp/downfall verified from `WilderWild-4.2.9-mc26.1.jar`; IDs are registry-stable across MC versions)
- **Traverse** 7 (`TraverseBiomes.java`)

Total custom entries now 137 (BoP 21 + Terralith 12 + Promenade 3 + the 101 new). `mod_version` → `1.4.0-beta.3+1.21.11`.

**Proof (vanilla headless atlas, seed 2533348776566713405, small/R7500/step16, JDK 21):** datapack loads with all entries present, worldgen generation-neutral — **50 distinct vanilla biomes, 0 non-`minecraft:` biomes appear, none vanished, equator jungle-dominant 82.4% not a monoculture (savanna present)** → absent-mod-safe + Art X clean.

**Per-mod status:** all four = **NEEDS-JAR-VALIDATION** — this machine's biome-mod jars are 26.1.2-only and won't load on the 1.21.11 runtime. IDs are real (from each mod's source/jar). 8 ambiguous placements flagged `NEEDS-JULIA-PLACEMENT-REVIEW` (see `# REVIEW` in the tool / RESULTS.md). Action for Julia: drop version-matched BYG/Terrestria/Wilder Wild(+FrozenLib)/Traverse jars in `run/mods`, re-run the atlas with `-Platdev.syncPreviewMods=true`.
