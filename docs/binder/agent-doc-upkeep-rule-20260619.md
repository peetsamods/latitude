# Agent doc-upkeep rule (2026-06-19)

`scope: repo operating instructions` · `status: recorded` · `repo: /Users/joolmac/CascadeProjects/Latitude (Globe)`

## Why this note exists

Julia pointed out that documentation tracking should be automatic, not something she has to request after every proof or readiness pass. The existing instructions required workers to read front-door docs first, but they did not explicitly require updating the handoff, binder, and lessons surfaces before calling non-trivial work complete.

## What changed

- `AGENTS.md` now has an `Automatic Documentation Upkeep` section.
- `docs/LESSONS.md` now has `L9 - Docs Coherence Is Part Of Done`.
- `docs/binder/README.md` lists this note as the newest binder entry.

## Durable rule

When non-trivial Latitude work changes current truth, evidence, blocker status, root/profile truth, release/readiness state, or the next resume gate, docs coherence is part of finishing the work. Future workers should update `docs/HANDOFF.md`, add or update a dated binder note, update the binder index, and add a lessons entry when a durable behavior rule changed.

This rule does not authorize unrelated cleanup, commits, pushes, releases, uploads, or live Minecraft work.
