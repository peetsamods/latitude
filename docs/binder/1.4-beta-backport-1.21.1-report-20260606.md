# 1.4 "Cohesive Horizons" worldgen backport (Stage 1) — Minecraft 1.21.1

`status: active` · scope: `worldgen` · workflow: `latitude-regression-fix-workflow`
Branch `port/1.4.0-beta-1.21.1`, baseline `port/1.3.0-1.21.1` (`71ffccef`, latest 1.3.0 head for 1.21.1). Mappings: Yarn `1.21.1+build.3`, loader `0.17.3`, fabric `0.115.6`, JDK 21.
Version: `1.4.0-beta.1+1.21.1`. Authored by the overnight backport thread (2026-06-06) per `docs/porting/HANDOFF-1.4-beta-backport.md`.

## Scope landed (Stage 1 / Bucket 1 — worldgen logic only)

This is an **algorithm transplant**, not a cherry-pick: the 1.4 source (26.1.2, Mojang mappings) was authored on a
**province/moisture** worldgen architecture that **does not exist in the 1.3.0 line** (no `ProvinceAuthority`,
no `Province` enum, no `enforceWarmProvinceFamily`, no moisture classifier). The pure-logic 1.4 changes were therefore
re-grounded onto the target's existing Yarn (`RegistryEntry<Biome>`) worldgen and validated map-based.

**Key porting fact:** the `1.21.1` 1.3.0 baseline `LatitudeBiomes.java` is **byte-identical** to the `1.21.11`
baseline (`diff` = 0 lines). The 1.21.11 transplant (proven, branch `port/1.4.0-beta-1.21.11`) therefore applies
**verbatim** — no Yarn-idiom adaptation was needed for this file. `compileJava` confirms the API touchpoints
(`RegistryEntry`, `Registry`, biome key lookup) resolve identically under the 1.21.1 Yarn mappings.

| 1.4 axis | source commit | status in 1.21.1 | how |
|---|---|---|---|
| Art VI `pickFrom` coherent noise | `da1bc0dd` | **LANDED (verbatim logic)** | `pickFrom` argmax over N coherent `ValueNoise2D` fields (`FALLBACK_COHERENCE_BLOCKS=128`, `FALLBACK_PICK_SALT`), replacing the `Math.floorDiv`+`hash64` cell-grid pick that was present and identical in this baseline. |
| Polar ice_spikes cap | `3f8e0366` | **LANDED (adapted)** | `keepPolarIceSpike` coherent cap (`POLAR_ICE_KEEP_THRESHOLD=0.45`, scale 256) in both `sanitizeLandBiome` overloads; source-source ice_spikes over the cap → `snowy_plains`. The baseline's `BAND_POLAR` block was *manufacturing* ice_spikes (forced forest/taiga→ice_spikes); rewired to `snowy_plains` (+`!path.contains("snowy")` guard) — Yarn idiom `getKey().map(k->k.getValue().getPath())`. |
| Equator overhaul: badlands gate + desert thinning | `91ffa425` + `10e10ffd` | **LANDED (re-grounded, province-independent)** | `demoteEquatorialBadlands`/`demoteEquatorialDesert` predicates collapsed into `demoteEquatorialDryBiomes` (both overloads), wired at the END of both `applyFinalSavannaClimateClamp` overloads before the savanna-tier pass. Keys on biome family + latitude ramp + coherent `ValueNoise2D` keep-noise (`BADLANDS_LAT_RAMP_LOW/HIGH=10/18`, `DESERT_LAT_RAMP_HIGH=12`, `DESERT_EQUATOR_KEEP_FRAC=0.40`). The WARM_DRY-province gate from the source is dropped (no province system here); the Earth-analog intent (no equatorial badlands; thinned-but-present equatorial desert; demote→savanna) is preserved exactly. |
| Warm-band rebalance (province wet-bias + WARM_WET variety + tier coherence 64→160 + badlands sub-province widen + `chooseBadlandsVariant`) | `8c73beab` | **DEFERRED (architecturally out of reach)** | Built entirely on `ProvinceAuthority.classifyWarm` / `enforceWarmProvinceFamily` / `Province` / moisture model — none exist in the 1.3.0 line. Porting it requires transplanting the whole province subsystem (Bucket-2-scale). The equatorial Earth-analog goal it targeted is delivered here by the province-independent equator gates above. `FALLBACK_COHERENCE_BLOCKS=128` is adopted; the `TIER_COHERENCE_BLOCKS=160` tier-coherence constant has no analog (no `pickFromWeighted` coherence tier in this baseline's selection path). |

## Proof (map-based, headless atlas, NO chunk pregen)

Seeds `2533348776566713405` + `1199119911991199`, `--size=small --radius=7500 --step=16 --emitbiomeindex=true`.
Metrics: `tools/atlas/sublatitude_dry_wet.py` + `tmp/tropical-badlands-gate-ace6e326/perbiome_sublat.py` (run from canonical repo).
**Note:** headless atlas is vanilla-biome-only (no BoP/Terralith/Promenade stack), so equatorial jungle share reads higher than the 26.1.2 BoP-stack figures; the §5 *qualitative* targets are what's validated.

Because the worldgen source is byte-identical to the 1.21.11 port, the per-band metrics are **bit-for-bit identical**
to the 1.21.11 figures on both seeds (verified independently by re-running the atlas on this branch, not copied).

### Per-band (seed 2533348776566713405 fix; % of land)
| band | badlands | desert | savanna | jungle |
|---|---|---|---|---|
| 0–5° | **0.00** | 5.45 | 6.61 | 82.37 |
| 5–10° | 0.00 | 1.37 | 7.50 | 87.62 |
| 10–15° | 0.00 | 1.10 | 6.83 | 86.92 |
| 24–30° | 0.01 | 1.99 | 33.97 | 57.84 |
| 30–35° (subtropical arid belt) | 0.00 | **54.98** | 23.68 | 0.26 |

Seed `1199119911991199` fix: 0–5° badlands **0.00%**, desert **0.00%**; 30–35° desert 53.92% (belt intact); equator jungle-dominant (91.8%) with savanna variety (5.1%) — not a monoculture.

### §5 targets — all met (both seeds)
- 0–5° badlands ≈ **0%** ✓ (0.00% both seeds).
- Deep-equator desert thinned ✓ (0–5° desert 5.45% seed1 / 0.00% seed2).
- Subtropical 20–35° arid belt broadly unchanged ✓ (30–35° desert ~54%).
- Equator jungle-dominant, NOT monoculture ✓ (82–92% jungle with savanna 5–10% present).
- Demoted dry → savanna; no biome vanishes (Art X) ✓.

### Build
- `compileJava` → BUILD SUCCESSFUL (JDK 21).
- `clean build` → BUILD SUCCESSFUL; `remapJar` produced `build/libs/latitude-1.4.0-beta.1+1.21.1.jar`.
- SHA-256: `333d8fcfeeee6b3616596df9bd2caecf624fd829743e19b56ca96ce65b860373`.
- Refmap regime: `globe.mixins.json` references `globe.refmap.json`; the 1.3.0 baseline build (mixinextras AP disabled in `build.gradle`) emits no embedded refmap — **identical to the shipped 1.3.0 line**, and this worldgen change touches only `LatitudeBiomes.java` (not a mixin), so it is refmap-neutral.

## Local-build note (do not regress)
`gradle.properties` `org.gradle.java.home` already points at the local macOS `temurin-21.jdk` on this baseline (unlike the 1.21.11 line's Windows path), so no temporary edit was needed. To rebuild:
`env -u JAVA_TOOL_OPTIONS JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew build`.
The committed `gradlew` exec bit was restored (`chmod +x`) so checkout builds work without manual intervention.

## Deferred / next
- Warm-band province subsystem port (Bucket 2 / later full-1.4 stage) — see table.
- Custom-biome source-wrap mixins + world-entry client (explicitly excluded from Stage 1).
- Julia: push release tag, GitHub pre-release (Beta), Modrinth Beta upload. (Branch pushed; `save/*` tag local-only.)

---

# Stage 2 — custom-biome TAG admission (`1.4.0-beta.2+1.21.1`, 2026-06-06)

Per handoff §8. Version bumped `1.4.0-beta.1 → 1.4.0-beta.2+1.21.1`.

## 2a. Custom-biome tags — PROVEN (absent-mod-safe + Art X), placement NEEDS-JAR-VALIDATION

Mod set for the 1.21.1 ecosystem (per §8): **BoP + Terralith + Nature's Spirit + Regions Unexplored + Promenade**.
**94 `required:false` entries** added across 11 `lat_*` tag files via `scripts/apply_custom_biome_tags.py`, climate-placed
by real-world analog. Breakdown by mod:
- **BoP / Terralith / Promenade** — the proven 26.1.2 placement (same IDs as the 1.21.11 set).
- **Nature's Spirit** — boreal_taiga/fir_forest/coniferous_covert → subpolar; aspen_forest/maple_woodlands → temperate;
  lavender/heather/marigold/carnation_fields + prairie → temperate accent; alpine_highlands/red_peaks → temperate_mountain;
  drylands/blooming_dunes → arid; arid_savanna/oak_savanna/chaparral + arid_highlands → savanna transition;
  bamboo_wetlands → tropics. IDs from the Nature's Spirit wiki (snake_case registry convention).
- **Regions Unexplored** — frozen_tundra/frozen_pine_taiga → polar_secondary; boreal/pine/blackwood_taiga → subpolar;
  deciduous/maple/silver_birch_forest → temperate; redwoods/willow/magnolia → temperate_secondary;
  flower/poppy/clover_fields + prairie → temperate accent; mountains/highland_fields → temperate_mountain;
  joshua/saguaro_desert/outback/arid_mountains → arid; baobab_savanna/steppe/dry_brushland → savanna transition;
  rainforest/sparse_rainforest/tropics/bayou/eucalyptus_forest → tropics. IDs verified from the published RU biome list
  (catter1 gist, v0.5.5). (The baseline tag taxonomy has no `lat_subtropical_humid_*` band, so RU bayou/eucalyptus
  home into `lat_tropics_secondary`.)

**PROVEN autonomously (headless atlas, vanilla — NO mods, NO pregen; seed `2533348776566713405`, small/R7500/step16):**
the datapack loads cleanly with all 94 `required:false` entries present; worldgen runs; world biome inventory =
**48 distinct biomes (the 1.21.1 vanilla set), zero non-`minecraft:` biomes, none vanished — Art X clean.** The tag
additions are **absent-mod-safe and generation-neutral** when the provider mods are absent.

**NEEDS-JAR-VALIDATION (for Julia):** the biome-appears-in-band/share proof needs the provider mods at runtime. **No
1.21.1-compatible BoP/Terralith/Nature's Spirit/Regions Unexplored/Promenade jars are on this machine** (the only
biome-mod jars present target MC 26.1.2). Notes for Julia: BoP, Terralith, Nature's Spirit, and Regions Unexplored all
publish 1.21.1 Fabric builds; **Promenade's published range is 1.21.4–1.21.11 and 1.20–1.20.2 — it appears to have NO
1.21.1 build**, so its `required:false` entries will simply stay inert on 1.21.1 (harmless). Drop the available
1.21.1 jars in `run-headless/mods` and re-run the atlas to confirm placement + share.

## Build
- `runBiomePreview` + `build` → BUILD SUCCESSFUL (JDK 21). Custom tags confirmed inside the remapped jar.
- Jar: `build/libs/latitude-1.4.0-beta.2+1.21.1.jar`
- SHA-256: `11984d982c4cce790f69aeaad228650b50354af9d04d7c2a742281c3bbe8b90b`

## 2b. Source-wrap mixins + world-entry client — see RESULTS.md (assessed across all versions).

---

## Stage 3 — broaden custom-biome tag admission (2026-06-06, HANDOFF §9)

Added 101 `required:false` `lat_*` worldgen tag entries for four more popular biome mods, on top of the Stage-2 BoP/Terralith/Promenade/Nature's Spirit/Regions Unexplored set, via `scripts/apply_custom_biome_tags.py`:
- **BYG** 51 (IDs from `Potion-Studios/BYG` `BYGBiomes.java`, overworld+beach; nether/end excluded)
- **Terrestria** 20 (`TerrestriaBiomes.java`)
- **Wilder Wild** 23 (temp/downfall verified from `WilderWild-4.2.9-mc26.1.jar`; registry IDs are stable across MC versions)
- **Traverse** 7 (`TraverseBiomes.java`)

Total custom entries now 195 (Stage-2 94 + 101 new). `mod_version` → `1.4.0-beta.3+1.21.1`.

**Proof (vanilla headless atlas, seed 2533348776566713405, small/R7500/step16, JDK 21):** datapack loads with all entries present, worldgen generation-neutral — **48 distinct vanilla biomes (matches the Stage-2 baseline — none vanished), 0 non-`minecraft:` biomes appear, equator jungle-dominant not a monoculture** → absent-mod-safe + Art X clean. (The `minecraft:leaf_litter` "Recoverable errors when loading section" in the log are stale pre-saved `run-headless/world` chunks from a newer-MC session, NOT a datapack/tag error; the fresh scan inventory is clean.)

**Per-mod status:** all four = **NEEDS-JAR-VALIDATION** (this machine's biome-mod jars are 26.1.2-only, won't load on the 1.21.1 runtime). IDs are real. 8 ambiguous placements flagged `NEEDS-JULIA-PLACEMENT-REVIEW`. Action for Julia: place version-matched jars in `run/mods`, re-run the atlas with `-Platdev.syncPreviewMods=true`.
