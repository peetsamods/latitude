# 1.4.1 (26.1.2) — overnight work summary for Peetsa

`date: 2026-06-22 (overnight, autonomous)` · branch `feat/custom-biome-expansion-26.1.2`

Worked the punch list autonomously per your "do as much as you can" + "Modrinth only, never the
Mojang launcher" instructions. Nothing was published, pushed, or committed — all changes are in the
working tree + the staging profile, fully reversible.

## TL;DR — what to do this morning
1. **Launch `Lat 1.4+26.1.2` in Modrinth and eyeball the snow line** (#1) — staged jar already has it.
2. **Pick a compass immersive direction (A/B/C)** from the mockup in chat (#4 themes already in the jar).
3. **Approve / redline the world-creation book layout** from the mockup in chat (#2 — no code yet).
4. **Set acceptance thresholds for #3 overrep** so the (already-written, risk-checked) tag-enrichment proposal can be applied + atlas-proven.

## Staged jar
`~/Library/Application Support/ModrinthApp/profiles/Lat 1.4+26.1.2/mods/latitude-1.4.1-beta.2+26.1.2.jar`
- SHA-256 `af0094f5b5e0ad825b370b3003c0b5c1bece3f2427cae19383460e1c4a45a572` — contains **#1 snow + #4 themes**.
- Previous jar backed up at `…jar.pre-snowline-20260621-221439` (restore that to revert both).

## #1 snow line — DONE (source-proven; needs your live eyeball)
Lowered the alpine snow-cap onset in the warm bands (climate-graded, see
`docs/binder/alpine-snowline-lower-20260622.md`): TEMPERATE 194→190, SUBTROPICAL 202→198, SUBPOLAR
187→186; POLAR (184) and TROPICAL (none) unchanged. `ALPINE_ROCK_Y=184` kept fixed → the
warm-latitude snow-creep guards stay authoritative and **no latitude that lacked snow gains any**.
Proof green: policy gates + compile + bytecode + unit-style onset logic. Existing chunks keep old
caps (legacy-pin); only new terrain uses the lower line.
- **Your gate:** temperate peak >Y190 should show a lower cap (~Y186–193); tropical peak stays bare stone. Easy to dial further.

## #4 compass — THEMES DONE (immersive styling needs your pick)
6 new analog themes added append-only (default `CLASSIC_GOLD` untouched, old configs load fine);
11 total, verified in the jar. See `docs/binder/compass-themes-20260622.md`. The "more immersive,
Minecraft-native" part is rendered as **3 directions (A current / B ticks+bezel / C compass rose)**
in chat — tell me which to build (all stay pixel-native, no anti-alias/glow, no HUD-overlap risk).

## #2 world-creation — MOCKUP for sign-off (no code yet, per "brainstorm first")
Squish root cause = `scaledUi()`/`compactUi()` are no-op stubs (`LatitudeCreateWorldScreen.java:532-538`)
→ fixed-pixel layout overlaps at high GUI scale. Book-UI 3-spread mockup (Identity / Planisphere /
Options, incl. new **Vanilla Amplified** type + restored **Random** band + Begin/Cancel on the spine)
rendered in chat. Responsive plan: book-rect fractions + a real `scaledUi`, single column per page,
one-leaf breakpoint. Full detail: `tmp/1.4.1-prep-20260621/ui/world-creation-bookui.md`.

## #3 overrepresentation — ANALYSIS LOCKED + PROPOSAL READY (held for your thresholds)
Re-analyzed the existing 30-run atlas matrix (no new builds). Confirmed robust offenders across all
sizes/seeds: `snowy_plains` ~67% of polar (95.8% polar duopoly w/ `ice_spikes`), `savanna` ~44%
subtropical; equator barely humid. Root cause = starved 1–2-entry primary tags × the fixed 70%
primary weight (NOT the confetti tier). A concrete **data-only tag-enrichment proposal** (which
biomes → which `lat_*` tags, climate-coherent, availability-checked) is written at
`tmp/1.4.1-prep-20260621/overrep/enrichment-proposal.md`, with the exact before/after atlas proof
commands. **Not applied** — it's a worldgen behavior change that needs (a) your acceptance
thresholds (proposed: ≤12% overall, ≤40% within-band, ≥60% tropical humid) and (b) the headless
before/after atlas proof. Jungle/tropics highs are by-design and must NOT be suppressed.
> **Adversarial verify (done): the data-only proposal is largely INERT on the main offenders.** The
> headline over-reps are hardcoded in Java, not the tags: `enforceWarmProvinceFamily` (~L8581) rewrites
> custom arid/savanna back to vanilla; WARM_DRY bypasses `lat_arid_*` (~L3854); the tropical leak is
> `pickOpenTropicalFallback` (~L7281); `snowy_plains` 66.7% polar is the `pickPolarWithFrontShoulder`
> remap (~L4881). This is *why #3 has been elusive*. The real fix is a Java slice (relax the enforcement
> to honor coherent custom ids + diversify the polar remap, Art X-guarded, proven per-province) — needs
> your thresholds + Java-vs-data-only call. I did NOT apply tag edits or burn a (likely-null) atlas run.
> Full write-up: `docs/binder/overrep-analysis-20260622.md`.

## Decisions I made for you (all reversible)
- Snow magnitude: moderate, real-climate-graded (you said "go with what you truly recommend").
- Overrep direction: data-only tag enrichment first (lowest risk) — but NOT applied without your thresholds.
- Compass: shipped the 6 palettes (clearly wanted); held the subjective immersive redesign for your pick.
- Did NOT touch #2 code (you wanted to brainstorm layout first).

## Governance / follow-ups
- Art II: snow + compass changes are in the 26.1.2 worktree; port to canonical + 1.21.1/1.20.1/1.21.11.
- legacy-worldgen-policy-pin: snow affects new chunks only (no save migration).
- Binder updated: evidence rows `20260622-alpine-snowline-lower` (worldgen), `20260622-compass-themes`
  (rendering); index worldgen + rendering topic rows refreshed.
- Nothing committed/pushed — your call on commits, version bump, and publication.

## How to revert
- Snow + compass jar: copy `…jar.pre-snowline-20260621-221439` back over the active jar.
- Source: `git checkout -- src/main/java/com/example/globe/world/LatitudeBiomes.java` (snow) and the 3
  `client/` compass files; or `git stash` the lot. Docs/tmp are additive.
